<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:Slugrace.Controls"
             x:Class="Slugrace.Controls.PlayersStats">

    <ContentView.Resources>
        <Style TargetType="Label" BasedOn="{StaticResource labelBaseStyle}" />
    </ContentView.Resources>
    
    <Grid
        RowDefinitions="*, *, *, *, *"
        ColumnDefinitions="*">
        <Label
            Text="Players' Stats"
            Style="{OnPlatform {StaticResource labelSectionTitleStyle},
                Android={StaticResource androidLabelSectionTitleStyle}}" />
        <local:PlayerStats 
            Grid.Row="1" />
        <local:PlayerStats 
            Grid.Row="2" />
        <local:PlayerStats 
            Grid.Row="3" />
        <local:PlayerStats 
            Grid.Row="4" />
    </Grid>
</ContentView>
